// const selectAmount = state => console.log(state);
//export const selectCategory = state => state.transaction;
//state.transaction.transactions.categories;

//state.auth.isLogin;
// const selectTransactions = state => console.log(state);
